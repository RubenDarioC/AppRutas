name: CI

on:
  push:
    branches:
      - 'Development'
      - 'feature/*'
      - 'fixbug/*'
  pull_request:
    branches:
      - 'Development'
      - 'feature/*'
      - 'fixbug/*'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      # Agrega tus otros pasos de construcción aquí

      # Ejemplo: Ejecutar pruebas
      - name: Ejecutar pruebas
        run: echo "Ejecutando pruebas..."

      # Ejemplo: Desplegar si las pruebas pasan
      - name: Desplegar si las pruebas pasan
        run: echo "Desplegando..."
        if: github.ref == 'refs/heads/master'
